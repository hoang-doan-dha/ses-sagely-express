extends layout

block content
  div(class="w-full h-32 flex items-center justify-center")
    h1(class="font-bold text-red-700 m-2 text-center text-3xl")= message
    h2= error.status
  pre(class="text-sm p-4") #{error.stack}
